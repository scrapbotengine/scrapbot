#!/usr/bin/env bash
set -euo pipefail

script_dir="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
cd "$script_dir/../.."

mkdir -p bin
out="bin/scrapbot-$$"
trap 'rm -f "$out"' EXIT

odin build src/scrapbot_cli -out:"$out"

set +e
"$out" "$@"
status=$?
set -e

exit "$status"
