[tools]
zig = "latest"

[tasks.build]
description = "Build the machina CLI"
run = "zig build"

[tasks.test]
description = "Run the full Machina test suite"
run = "zig build test && zig build && ./zig-out/bin/machina test tests/projects && ./zig-out/bin/machina render-test examples/minimal zig-out/minimal-render-test.bmp"

[tasks.luau-check]
description = "Run Luau editor type checks"
run = "bash tools/check-luau-types.sh"

[tasks.render-test]
description = "Render and verify the minimal example scene"
run = "mise machina render-test examples/minimal zig-out/minimal-render-test.bmp"

[tasks.machina]
description = "Build and run the machina CLI"
run = "mise build && ./zig-out/bin/machina"
