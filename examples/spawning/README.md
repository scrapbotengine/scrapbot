# Spawning Demo

This example starts with no scene-authored renderable entities. The camera and
light are text-authored, then `scripts/gameplay.luau` runs a startup system that
spawns visible mesh entities and adds their components programmatically.
