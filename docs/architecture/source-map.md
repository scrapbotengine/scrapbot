# Source Map

**Last verified:** 2026-08-13

| Path | Responsibility | Important boundaries |
| --- | --- | --- |
| `src/scrapbot_cli/main.odin` | CLI entry point and command dispatch. | Human/JSON diagnostics delegate into engine packages. |
| `src/scrapbot/live_debug/` | Transport-independent live diagnostic snapshots, bounded capture jobs, discovery, and the loopback HTTP JSON/CBOR adapter. | The network worker never traverses ECS, resources, renderer, or GPU state; engine-thread publishers provide immutable snapshots and consume bounded commands. |
| `src/scrapbot/scrapbot.odin` | Runtime composition, native/Luau scheduling, profiling, project load/run orchestration. | Joins registries, executor, resources, renderer, and hot reload. |
| `src/scrapbot/resource_residency.odin` | Scene dependency ownership, active/staging closure admission, grace eviction, and declaration retention. | Owns project-resource CPU residency above the registry; does not own ECS worlds or backend caches. |
| `src/scrapbot/shared/` | Cross-package POD types, UUIDs, transforms, camera math, public world shapes, and canonical UI theme vocabulary/resolution. | Avoid backend-owned objects and allocator-sensitive ABI leakage. Theme recipes may produce only ordinary public UI component values. |
| `src/scrapbot/component/` | Component registry and generated Luau declarations. | Canonical component names, ownership, storage kind, lifecycle, and field schemas. |
| `src/scrapbot/file_browser/` | Rooted, filtered, bounded filesystem metadata navigation for engine tooling. | Retains no file payloads, rejects escapes and symlink traversal, and owns no open/save/import/delete policy. Refresh and navigation are explicit transactions. |
| `src/scrapbot/ecs/` | World storage, typed mutation, commands, authoring snapshots, hierarchy, independently scaled project clocks, transient editor-camera point-of-interest navigation, integrity, UI storage, and bounded immutable UI event history. | All mutation must preserve indexes and publish structural/render/UI invalidation. Project clocks advance only on permitted simulation steps; editor-camera orbit state remains internal and per-run; event readers never mutate or drain the history. |
| `src/scrapbot/project/` | Project/scene parsing, recursive UUID scene catalog and resource-closure discovery, UI theme authoring resolution, resource discovery, fonts, configuration, recoverable save transaction. | Persistent source identity and validation. Scene catalog metadata retains dependency-first UUID closures but no entity payloads; only the selected scene payload becomes an ECS World. Theme directives are consumed before explicit component fields. |
| `src/scrapbot/asset_import/` | Incremental source-asset importers, common product envelopes and sequential writers, atomic products, SVG-to-MTSDF icon compilation, texture mips, static glTF decoding/LOD/SDF compilation, split streaming Model products, and HDR-to-IBL preprocessing. | Source/dependency/settings fingerprints and versioned `.scrapbot/imported/` products; typed chunk ranges are validated before importer decoding, Model catalogs/images/coarse pages/detail pages/distance fields are independently addressable, and source conversion is never ordinary-frame work. |
| `src/scrapbot/geometry/` | Backend-independent crack-aware hierarchy construction, boundary-loop preservation, validation, cloning, deterministic self-contained vertex/index page construction, error-prioritized bootstrap-tail selection, and offline mesh-distance-field compilation. | Imported products and runtime Geometry use one hierarchy representation; terminal groups at any depth form the pinned fallback, while a bounded reachable refinement tail supplies evictable startup detail. Distance fields classify watertight topology before assigning a sign. None of this construction is ordinary-frame work. |
| `src/scrapbot/resources/` | Runtime geometry/texture/environment/icon-set/model/shader/material/font/UI-theme registries, project-payload retirement, generational handles, imported/generated LOD attachment, source- or leaf-derived meshlets, position-only query proxies, and file-or-memory Geometry page sources. | Shared descriptions outside ECS; compact imported catalogs publish and physically release entries incrementally. LOD, query proxy, meshlet, hierarchy, and page-source data share exact Geometry lifetime/versioning. File-backed canonical views are temporary consumer-owned reconstructions; UI/backend caches consume render-resource versions, while theme versions serve explicit composition and editor inspection only. |
| `native/clusterlod/`, `native/distance_field/`, `third_party/meshoptimizer/` | Narrow Scrapbot C bridges plus pinned upstream geometry simplification, canonical-position boundary detection, clustering, crack-aware cluster-LOD, bounds, and BVH-accelerated mesh-distance-field construction. | Model LOD, imported hierarchy, and distance-field construction are invalidated asset work; non-imported hierarchy construction runs at Geometry registration/replacement. Native result ownership crosses only explicit copy/free boundaries. None is ordinary-frame work. |
| `src/scrapbot/schedule/` | Access-derived plan and native worker executor. | Native parallel batches, conflicts, and serial barriers. |
| `src/scrapbot/script/` | Luau VM, public APIs, schemas, queries, write-back, UI theme/component composition, UI event snapshots, generated-type integration. | Theme resolution returns mutable ordinary component maps; event cursors are per consumer; deferred lifecycle and declared-write enforcement remain at attachment. |
| `src/scrapbot/extension_api/` | Raw C-compatible native extension ABI. | Fixed layouts and callbacks only, including host-owned UI theme resolution and bounded UI event copies into caller buffers. |
| `src/scrapbot/extension/` | Idiomatic Odin wrapper for extension authors. | Typed descriptors, UI theme recipes, and payloads over the raw ABI. |
| `src/scrapbot/native/` | Native extension building, loading, registration, callbacks, UI theme/component bridging. | Host validation, dynamic-library lifetime, and per-system command buffers. |
| `src/scrapbot/ui/` | Retained ECS UI, physical-input adapters, semantic editor actions, intrinsic/flex/dock layout, consumable interaction propagation, editor ECS composition, resource-directory presentation, runtime component-payload inspection/bindings, play-edit staging, placement feedback, diagnostics, fonts. | One adapter derives authoritative UI pointer edges and editor actions from the committed ECS input snapshot. Layout and paint share active-font line breaking; wrapped flex and direct-child dock-tab work are bounded and revision-driven. Topmost controls capture presses and cancel pointer activation before an interactive ancestor can reuse it. Dock transfers mutate public UUID parents and publish generic events. The Resource Browser joins metadata-only rooted directory entries with registry records by source directory; laid-out renderable rows and selected Inspector previews share the existing bounded viewport pool, while other rows use atlas icons. Semantic diagnostics emit input plus one-shot editor-camera pose commands, target inspector controls through reflected component/field/axis bindings, assert transport/dirty/history/staging state, and expose those facts with the ordered selected UUID set in UI dumps; render orchestration applies camera commands and fulfills backend selection requests. Active Model drags retain generic source/target/pointer state while editor orchestration owns placement meaning and overlay feedback. Completed eligible play edits replace exact UUID/component snapshots in a transient journal; entity/component project-system provenance drives eligibility and removes late conflicts before baseline restoration. Editor transport, eligibility labels, recipes, and workspace visibility compose and mutate the shared public contract. |
| `src/scrapbot/render/` | Backend interface, run-global engine time, null backend, surface/offscreen WGPU rendering, project-shader composition, per-Shader spectral-surface FFT fields, sorted transparency, shared geometry arenas, bounded virtual-geometry residency, GPU-driven object/meshlet visibility, exact scene queries, on-demand rendered-identity selection, editor tools and feedback, embedded UI viewports, postprocess, profiling, and live-debug artifact providers. | Backend-neutral nearest-hit queries return world contact data for placement and tools. WGPU selection instead reuses retained scene submission and exact material/custom-vertex coverage in a scissored `R32Uint` pass, snapshots slot-to-root UUID ownership, and maps a three-slot readback ring asynchronously; ordinary frames pay no selection-pass cost. Strict projected-bounds containment remains CPU-derived and intersects with returned visible UUIDs. Camera/light overlays resolve before geometry selection. Retained selected slots independently drive the depth-free outline mask. Other WGPU caches consume temporary canonical views only at invalidation and retain versioned, bounded ownership. |
| `src/scrapbot/platform/` | SDL window/input/cursor integration for visible runs. | OS events map only to the canonical physical input snapshot plus focused text composition/repeated navigation. Editor shortcuts, transform chords, camera movement, and pointer ownership resolve above this boundary. SDL still provides relative mouse mode, cursor shape, window confinement, and transform edge wrapping; offscreen WGPU does not initialize this boundary. |
| `src/scrapbot/hot_reload.odin` | Project source/product change detection, active-scene preservation, queued scene replacement, residency replacement, and safe runtime replacement. | Failed reload/transition retains last-good runtime/world; replace transitions retain active/staging resource closures and bound world overlap to active plus candidate. |
| `src/scrapbot/playback.odin` | Play/Stop baseline capture and restoration. | Restores ECS/resource authoring state without reloading code. |
| `src/scrapbot/scene_*.odin`, `project_save.odin` | Scene serialization, semantic/structural patching, project-wide persistence. | Stable UUID targeting and recoverable multi-file commits. |
| `src/scrapbot/package.odin` | Packaged-project product assembly. | Separates source state from build products. |
| `docs/adr/`, `docs/fdr/`, `docs/architecture/` | Decisions, feature contracts, and current source map. | Keep rationale, behavior, and inventory distinct. |
| `docs-website/` | Public user documentation. | Canonical user-facing APIs and workflows. |
| `tools/analyze_render_profile.mjs`, `tools/profile_resolution_sweep.mjs`, `tools/run_gpu_benchmarks.mjs`, `tools/compare_gpu_benchmarks.mjs`, `tools/test_gpu_offscreen.mjs` | Agent-oriented render-profile analysis, bounded pixel-cost sweeps, historical benchmark bundles/comparison, and artifact-preserving offscreen GPU acceptance. | Consume or orchestrate bounded renderer runs; never participate in ordinary engine frames. Hardware histories compare only adapter- and dimension-compatible evidence. |

## Dependency direction

- `shared` defines neutral data used across engine packages.
- `component`, `ecs`, `project`, and `resources` define runtime data and ownership.
- `script` and `native` adapt project-authored behavior onto those contracts.
- `ui` and `render` consume ECS/resources while retaining derived state behind explicit invalidation.
- `live_debug` owns transport and synchronized diagnostic copies without importing engine owners; the root runtime and renderer publish into it.
- the root runtime composes lifetimes and frame order; the CLI remains outside engine internals.

Avoid importing renderer/backend details into public ECS components, project resources, scripting payloads, or native ABI types.
